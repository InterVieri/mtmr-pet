# icons 素材目录

素材不含在仓库（私人/第三方版权），按下面结构自备：

- `chars/`：角色 GIF（任意 `.gif`，拖进来调试器自动拆帧 + 重做帧 + 重启生效）
- `back/`：背景图（PNG/JPG，至少 1 张）
- `frames/`：GIF 拆出的帧备用（可空）
- `walk/`：调试器「重做帧」自动生成的合成帧（运行时生成，勿手改）

首次跑：把你的角色 GIF 放进 `chars/`、背景图放进 `back/`，调试器读它们生成 `walk/` 的帧。`walk/` 留空即可（调试器自动写）。
